// src/components/Load.jsx
import React from 'react';

const Carga = ({ name }) => {
    return (
        <div>
            <h4>{name}</h4>
            <p>Consumo: 100kW</p>
        </div>
    );
};

export default Carga;
